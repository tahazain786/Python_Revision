class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        n = len(nums)
        if n <= 1:
            return n
        k = 0
        for i in range(1,n):
            if nums[k] != nums[i]:
                k+=1
                nums[k] = nums[i]
        return k+1

arr = [1,2,3,7,5,10,1,1,20]
target = 12
i = 0
psum = 0
for j in range(len(arr)):
    psum += arr[j]
    
    while psum > target:
        psum -= arr[i]
        i += 1
    if psum == target:
        print(i,j)

class Solution:
    def twoSum(self, numbers: List[int], target: int) -> List[int]:
        mappy = {}
        for i in range(len(numbers)):
            diff = target - numbers[i]
            if diff in mappy:
                return [mappy[diff]+1,i+1]
            mappy[numbers[i]] = i
        return []
